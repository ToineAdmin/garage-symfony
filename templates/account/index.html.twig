{% extends 'base.html.twig' %}

{% block title %}Mon compte - Garage V.PARROT{% endblock %}

{% block content %}
<div class="container mt-5">
    <h1> Mon compte </h1>
    <p>Bienvenue {{ app.user.firstname }} ! Vous pouvez administrer de site depuis cet espace.</p>

    {% if is_granted('ROLE_ADMIN') %}
        <p>Vous êtes l'administrateur <a href="/admin" class="btn btn-primary">  accéder au backoffice</a> ?</p><br/>
    {% endif %}
    <hr>
    <div class="row mb-5">
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{asset('assets/img/cadenas.png')}}" alt="Modifier mon mot de passe"><br/>            
                <a href="{{path('account_password')}}">Modifier mon mot de passe </a><br>
            </div>
        </div>
    </div>
</div>
{% endblock %}
